/** Docs-index loader options (fs-free, shared by the loader and provider). */
export type DocsIndexLoaderOptions = {
  /** Override docs-index root directory (useful for tests). */
  docsIndexDir?: string;
};
